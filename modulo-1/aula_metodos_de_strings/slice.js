const string = 'Cubos Academy'

console.log(string.slice(4)); // corta os caracteres iniciais da string
console.log(string.slice(-4)); // corta a string deixando os ultimos caracteres

console.log(string.slice(3, 5));
console.log(string.length);

