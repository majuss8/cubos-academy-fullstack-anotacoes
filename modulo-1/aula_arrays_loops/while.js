const nomes = ['jose', 'joao', 'maria', 'joana', 'andre'];

let indice = 0;

while (indice < nomes.length) { // enquanto o que tiver entre () for verdadeiro, execute esse bloco de código
    console.log(nomes[indice]);
    indice++;
}